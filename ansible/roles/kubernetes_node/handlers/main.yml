---
- name: Apply Kubernetes sysctl parameters
  ansible.builtin.command:
    cmd: /usr/sbin/sysctl --load=/etc/sysctl.d/99-kubernetes.conf

- name: Restart containerd
  ansible.builtin.systemd_service:
    name: containerd
    state: restarted
